# Memlin Claude Code marketplace

Install in Claude Code:

```
/plugin marketplace add <this-directory-or-repo>
/plugin install memlin
/memlin-login
```

This directory is auto-generated from the private Memlin repo. Source lives at
`apps/cli-plugin/` in `github.com/memlin-ai/memlin`. Do not edit files here
directly — they get clobbered on every rebuild.
